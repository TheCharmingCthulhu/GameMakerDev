enum Blocks{
    T,
    L,
    I,
    O
}

blocks[0] = instance_create(x, y, obj_block);
blocks[1] = instance_create(x, y, obj_block);
blocks[2] = instance_create(x, y, obj_block);
blocks[3] = instance_create(x, y, obj_block);

return blocks;
 
